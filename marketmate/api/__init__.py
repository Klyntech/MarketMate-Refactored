"""
marketmate.api
───────────────
HTTP API layer — FastAPI routers for auth, core routes, and academy.

Packages:
  auth   — Authentication routes and session utilities
  routes — Core application routes (health, trades, performance, webhook)
"""
